<Window x:Class="Client.Views.Dialogs.RenameFolderDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Переименовать папку" 
        Height="200" 
        Width="400"
        WindowStartupLocation="CenterOwner">

    <StackPanel Margin="20">
        <TextBlock Text="Новое название папки:" Margin="0,0,0,10"/>
        <TextBox x:Name="FolderNameTextBox" Text="{Binding CurrentName, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="0,0,0,10"/>

        <TextBlock x:Name="ErrorText" Foreground="Red" Visibility="Collapsed" Margin="0,0,0,10"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Width="80" Margin="0,0,10,0" Click="CancelButton_Click"/>
            <Button Content="Переименовать" Width="100" Click="RenameButton_Click"/>
        </StackPanel>
    </StackPanel>
</Window>