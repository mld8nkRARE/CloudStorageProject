<UserControl x:Class="Client.Views.Auth.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F0F2F5">
    <Grid>
        <Border Width="350" Padding="20" Background="White" CornerRadius="10">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="Вход в аккаунт" FontSize="20" FontWeight="Bold" 
                           HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <TextBlock Text="Email:"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Width="300" Margin="0,5"/>

                <TextBlock Text="Пароль:" Margin="0,10,0,0"/>
                <PasswordBox Width="300" Margin="0,5" PasswordChanged="PasswordBox_PasswordChanged"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button Content="Войти" Command="{Binding LoginCommand}" Width="120" Margin="0,0,10,0"/>
                    <Button Content="Регистрация" Command="{Binding OpenRegisterCommand}" Width="120"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>



