<UserControl x:Class="Client.Views.FolderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F0F2F5">
    <Grid Margin="20">
        <Border Background="White" CornerRadius="10" Padding="15">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
            </Border.Effect>

            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,10">
                    <Button Content="Создать папку" Width="120" Margin="0,0,10,0"
                            Command="{Binding CreateFolderCommand}"/>
                    <Button Content="Удалить" Width="120"
                            Command="{Binding DeleteItemCommand}"/>
                </StackPanel>

                <TextBlock Text="{Binding CurrentFolderName}" FontWeight="Bold" Margin="0,0,0,10"/>

                <ListView ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}"
                          MouseDoubleClick="ListView_MouseDoubleClick">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Название" DisplayMemberBinding="{Binding Name}" Width="200"/>
                            <GridViewColumn Header="Тип" DisplayMemberBinding="{Binding Type}" Width="100"/>
                            <GridViewColumn Header="Размер" DisplayMemberBinding="{Binding Size}" Width="100"/>
                            <GridViewColumn Header="Дата создания" DisplayMemberBinding="{Binding CreatedAt}" Width="150"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>



